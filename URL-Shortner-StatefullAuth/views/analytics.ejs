<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>URL Analytics</title>
</head>
<body>
    <h1>URL Shortener Analytics</h1>
    <p>Short URL: <a href="/url/<%= id %>">http://localhost:8001/url/<%= id %></a></p>
    <p>Total Clicks: <%= totalClicks %></p>
    <ul>
        <% analytics.forEach((visit) => { %>
            <li><%= new Date(visit.timestamp).toLocaleString() %></li>
        <% }) %>
    </ul>    
</body>
</html>
